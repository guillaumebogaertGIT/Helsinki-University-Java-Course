This project provides local build/run scripts so you can compile without Maven.

Windows:

Run `build.bat` to compile sources and produce `target\Part04_15.Statistics.jar`.
Run `run.bat` to compile and run the `Main` class.

Unix:

Run `./build.sh` and `./run.sh` (if provided) — not included here but similar logic applies.
